# Provenance and contribution disclosure

Prepared 2026-09-10 at the user's request as a separate submission directory.
The original research lives in inquiries 44j/44l (classification and formal
negative construction), 44h (prefix repair), and 44m/44n (universality and
observation consequences). This package is a self-contained snapshot, not a
set of links into the research vault.

The human user selected Alexander's paper, directed continued research, and
requested the author and submission packages. OpenAI Codex, described in this
session as based on GPT-6, developed the new construction, proofs, formalization,
checks and exposition. No more specific deployment identifier or model-role
history is invented here. No independent human mathematical review has been
recorded. Publication identity and attribution remain for the user to choose.

`BiologicalAvoidance.lean` is the original checked 44l source with an updated
header and its print commands moved into `Audit.lean`. `RealBirthdates.lean`
adds the real-birthdate bridge in this packaging pass. The negative theorem's
population hypotheses are proved, not introduced as external axioms.

The dependency manifest comes from the previously used Lean environment,
with the project name changed and the mathlib input revision pinned to its
full commit. The portable verifier accepts any matching dependency checkout;
the local run explicitly records reuse of existing compiled dependency caches.

No private correspondence, original chat transcript, access tokens or user
identity information is included. No external publication or message has been
sent. `submission.json` contains unfinished public-URL and identity fields,
rather than invented publication details.
